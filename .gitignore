## .NET build outputs
bin/
obj/
out/
publish/
*.user
*.suo
*.rsuser

## .NET tool outputs
.vs/
.vscode/
.idea/
*.swp
*~
# AOT stubs staged by unity/scripts/copy-stubs.sh — built artifacts, not source.
# CI re-stages them from the per-RID AOT artifacts before zipping the package.
unity/Samples~/ArcadeBasic/Stubs/
.DS_Store

## Test outputs
TestResults/
coverage/
*.trx
*.coverage
*.coveragexml

## NuGet
*.nupkg
*.snupkg
.nuget/
project.lock.json

## Verify snapshot received files (kept files have .verified.* suffix)
*.received.*

## Local config
*.local.json

## Reference documents — kept locally, not in git
## (spec PDF likely copyrighted; NBS PDFs public domain but large)
/full-basic-standard.pdf
/NBS Minimal BASIC Test Programs*.pdf

## Unity package: DLLs are generated by `unity/scripts/copy-dlls.sh` or by
## the release CI; never check them into the repo. The release artifact
## bundles them when zipping the package.
unity/Runtime/Plugins/*.dll
unity/Runtime/Plugins/*.dll.meta
unity/Runtime/Plugins/*.pdb
